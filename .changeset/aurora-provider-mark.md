---
"@tesserix/web": minor
---

Export `AuroraProviderMark` so a host with its own button chrome — a Zitadel login form whose submit button carries the server action and pending state — can still render the resolved brand logo.
