---
"@tesserix/web": patch
---

Expose `inputBorder` on the aurora palette and `--aurora-input-border` on the panel, so host form controls can pick up a brand-tinted field border instead of a neutral one.
