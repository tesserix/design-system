---
"@tesserix/otto-widget": patch
---

Otto chat — audible reply notification: the customer widget now plays a short chime when Otto (or a human agent) replies, so a minimized chat still alerts the customer that a response arrived. No-op if the browser blocks autoplay. (The staff OttoInbox already chimes on new pending threads.)
