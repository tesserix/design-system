# useDebounce

Package: `@tesserix/hooks`

Hook that debounces a value

## Import

```tsx
import { useDebounce } from '@tesserix/hooks'
```

## Source

- Hook: `packages/hooks/src/use-debounce.ts`

## Usage

```tsx
import { useDebounce } from '@tesserix/hooks'

export function DemouseDebounce() {
  const debounced = useDebounce(search, 300)
  return null
}
```
